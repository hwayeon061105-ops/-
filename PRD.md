# PRD: 운동 동기부여 AI 조언 서비스 (1페이지 MVP)

---

## 1) 목표와 성공조건

### 1.1 제품 목표
* 사용자가 키, 몸무게, 현재 마음가짐을 입력하고 원하는 조언 모드를 선택하면, AI가 분석하여 이모티콘이 포함된 3줄 분량의 맞춤형 조언을 제공하여 운동 실행을 유도한다.
* 단일 화면(Single Page) 구조로 구성하며, 로딩 애니메이션 및 예외 처리를 통해 안정적인 사용자 경험을 제공한다.

### 1.2 성공 조건 (KPI)
1. **개발 완수:** 지정된 범위 내 핵심 기능(입력-검증-AI 처리-3줄 출력) 및 상태별 UI(로딩, 에러) 연동 완료.
2. **응답 만족도:** AI 요청 후 5~10초 이내에 정상적으로 3줄 조언이 화면에 출력되는 비율 95% 이상.
3. **재시도/재이용률:** 결과 화면에서 '다시 하기' 또는 '재요청' 버튼을 통해 서비스를 재이용하는 비율 20% 이상.

---

## 2) 사용자 스토리

* **US-01 (쓴소리):** 운동 가기 싫을 때 쓴소리를 듣고 싶다. 왜냐하면 오기로라도 운동을 하러 가고 싶기 때문이다.
* **US-02 (따뜻한 공감):** 처음 마음가짐과 달리 의욕이 떨어졌을 때 따뜻한 조언을 듣고 싶다. 왜냐하면 처음 마음가짐으로 돌아가 다시 열심히 운동하고 싶기 때문이다.
* **US-03 (현실 직시):** 운동을 회피하고 싶을 때 현실적인 조언을 듣고 싶다. 왜냐하면 현실을 직시하고 스스로를 돌아보며 운동할 마음을 내고 싶기 때문이다.

---

## 3) 화면 명세

### 3.1 화면 구조 (단일 화면 Layout)
* **상단:** 서비스 타이틀 및 소개 문구
* **중단 (입력 폼 영역):**
  1. **신체 정보 입력란:** 키(cm) 입력 필드, 몸무게(kg) 입력 필드
  2. **현재 마음가짐 입력란:** 텍스트 입력 필드 (Placeholder: `"지금의 심정을 적어보세요"`)
  3. **검증 안내 문구 영역:** 미입력 필드 발생 시 해당 입력창 하단에 빨간색 글씨로 안내 표시 (예: `"키 칸이 비어있어요"`)
  4. **조언 옵션 선택 그룹 (3가지 중 1개 선택):**
     * 쓴소리
     * 현실직시
     * 따뜻한 공감
  5. **Primary CTA 버튼:** `조언 받기`
* **로딩 상태 레이아웃 (처리 중):**
  * `조언 받기` 클릭 후 결과 출력 전까지 화면 중앙에 **조금씩 움직이며 운동하는 캐릭터 애니메이션** 노출.
* **하단 (결과 영역 - 생성 완료 후 노출):**
  1. **조언 출력 카드:** 선택한 옵션에 맞춰 이모티콘/특수문자 텍스트가 포함된 **정확히 3줄**의 조언 메시지 출력.
  2. **Control 버튼 그룹:**
     * `다시 하기` (입력 폼 상태로 복귀 및 초기화)
     * `원하는 느낌 나올 때까지 다시 생성` (동일 조건으로 AI 재요청)

---

## 4) 기능 명세

### 4.1 입력 및 유효성 검증
* 키, 몸무게, 현재 마음가짐 입력 필드와 조언 옵션(쓴소리 / 현실직시 / 따뜻한 공감)을 제공.
* 제출 시 각 필드의 공백 여부를 검증. 비어있는 항목이 존재할 경우 AI 요청을 진행하지 않고 즉시 빨간색 안내 문구 노출.

### 4.2 AI 조언 요청 및 프롬프트 제어
* 사용자가 입력한 데이터(키, 몸무게, 현재 심정)와 선택한 옵션(쓴소리/현실직시/공감)을 AI 엔진으로 전달.
* **프롬프트 제약 조건:**
  * 반드시 **이모티콘 또는 표정 모양 특수문자**를 문장에 포함할 것.
  * 결과물은 줄바꿈(`\n`) 기준 **정확히 3줄**로 응답할 것.
* **응답 시간 범위:** AI 처리 후 5~10초 사이에 결과를 화면에 렌더링.

### 4.3 상태 관리 및 내비게이션
* **처리 중 상태:** 요청 시작과 동시에 '운동하는 캐릭터 애니메이션'을 활성화하고 버튼을 비활성화 처리.
* **새로고침:** 브라우저 새로고침 시 모든 상태가 초기화되어 첫 입력 페이지로 돌아감.
* **뒤로 가기 / 다시 하기:** 결과 화면에서 입력 화면으로 되돌아가는 네비게이션 제공.

---

## 5) 예외 처리

> AI 연동 및 사용자 입력 오류 발생 시 화면이 멈추거나 깨지지 않도록 아래 예외 규칙을 엄격히 적용합니다.

1. **미입력 필드 존재 시 (Field Blank Exception)**
   * 입력칸 중 1개라도 비어있는 상태에서 `조언 받기` 클릭 시:
   * AI 처리 단계로 넘어가지 않고, 비어있는 항목을 구체적으로 지목하여 빨간색 글씨로 안내문구 표시 (예: `"몸무게 칸이 비어있어요"`, `"현재 마음가짐 칸이 비어있어요"`).
2. **AI 응답 실패/오류 발생 시 (AI API Failure Exception)**
   * 네트워크 오류, API Limit 초과, 타임아웃 등으로 AI 응답 실패 시:
   * 전체 화면이 다운되지 않도록 Catch 처리하며, 결과 영역에 `"조언을 불러오지 못했어요 😢"` 에러 안내와 함께 `다시 시도하기` 버튼 노출.
3. **AI 응답 형식 불일치 시 (Formatting Mismatch Exception)**
   * AI가 3줄이 아닌 장문이나 단문(1~2줄, 4줄 이상)으로 응답을 반환한 경우:
   * 클라이언트 측에서 줄바꿈 기준으로 파싱하여 3줄 규격으로 자르거나, 재요청 로직을 자동 수행하여 3줄 형태를 유지.
4. **특수문자/XSS 입력 시 (Sanitization Exception)**
   * 현재 마음가짐 입력란에 HTML/스크립트 태그가 포함된 경우:
   * AI 프롬프트 전달 및 화면 출력 시 이스케이프(Escape) 처리하여 보안 사고 및 출력 깨짐 방지.
5. **연속 클릭 / 중복 제출 시 (Debouncing Exception)**
   * 로딩 상태(운동하는 캐릭터 동작 중)일 때 `조언 받기` 버튼을 중복 클릭할 수 없도록 버튼 Pointer-events 차단 및 Disabled 상태 유지.

---

## 6) 완료 조건

* [ ] 키, 몸무게, 현재 마음가짐 입력 후 버튼 클릭 시 5~10초 사이에 결과가 표시되는가?
* [ ] 입력 항목이 비어있을 경우 빨간 글씨로 `"00칸이 비어있어요"` 문구가 뜨고 다음 단계로 넘어가지 않는가?
* [ ] 처리 중(5~10초) 동안 조금씩 움직이며 운동하는 캐릭터 애니메이션이 렌더링되는가?
* [ ] 결과 조언에 이모티콘/표정 특수문자가 포함되어 있으며, 정확히 3줄로 출력되는가?
* [ ] AI 응답이 실패하더라도 화면이 멈추지 않고, '다시 하기' 또는 재시도 버튼이 정상 동작하는가?
* [ ] 새로고침 시 초기 입력 페이지로 정상 되돌아가는가?